// Reader overlays should start at the document header's bottom border:
// global protected layout top offset (65px) + reader header height (80px).
export const READER_OVERLAY_TOP_OFFSET_PX = 145;
export const READER_OVERLAY_TOP_CLASS = "top-[145px]";
